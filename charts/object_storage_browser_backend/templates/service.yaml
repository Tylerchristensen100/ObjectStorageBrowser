apiVersion: v1
kind: Service
metadata:
  name: {{ include "object_storage_browser_backend.fullname" . }}
  labels:
    {{- include "object_storage_browser_backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "object_storage_browser_backend.selectorLabels" . | nindent 4 }}
