apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "object_storage_browser_frontend.fullname" . }}-test-connection"
  labels:
    {{- include "object_storage_browser_frontend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "object_storage_browser_frontend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
